# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    stripe_api_secret_key: '%env(resolve:STRIPE_API_SECRET_KEY)%'
    stripe_api_public_key: '%env(resolve:STRIPE_API_PUBLIC_KEY)%'
    stripe_webhook_secret: '%env(resolve:STRIPE_WEBHOOK_SECRET)%'
    stripe_api_version: '2022-08-01'

    subscription.price:
        1: price_1LrfXXJMwMCAol8z9v1fYovm
        2: price_1LrfXlJMwMCAol8zdU0ARHzw
    subscription.product:
        1: prod_MarG0M8Hq0z55D
        2: prod_MarGh7ueVfifKF

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    App\Service\StripeService:
        arguments:
            $api_key: '%stripe_api_secret_key%'
            $api_version: '%stripe_api_version%'

    App\EventListener\UserValidatedEventListener:
        tags:
            - { name: kernel.event_listener, event: app.user.validated }

    App\Security\Voter\SubscriptionVoter:
        arguments:
            $subscriptionProducts: '%subscription.product%'